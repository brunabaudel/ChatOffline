<?xml version="1.1" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.ufpe.cin.if1001.chatoffline" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/MyMaterialTheme" >
        <activity
            android:name=".gui.base.initial.InitialActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".gui.base.activity.MainActivity" >
        </activity>

        <activity android:name=".gui.base.initial.UsernameActivity" >
        </activity>

        <activity android:name=".gui.message.MessageActivity" >
        </activity>

        <service
            android:name=".controllers.services.ChatService"
            android:enabled="true"
            android:exported="false" >
        </service>
    </application>

</manifest>
